@model MvcOrder.Models.Order

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Order</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <!--       <div class="form-group">
                   <label asp-for="Name" class="control-label"></label>
                   <input asp-for="Name" class="form-control" />
                   <span asp-validation-for="Name" class="text-danger"></span>
               </div>
    -->
            @Html.DropDownList("Name", new List<SelectListItem>

           { new SelectListItem { Text = "Screws" , Value = "Screws" },

             new SelectListItem { Text = "Bolts" , Value = "Bolts" },

             new SelectListItem { Text = "Scissors" , Value = "Scissors" },

             new SelectListItem { Text = "Hammer" , Value = "Hammer" },

             new SelectListItem { Text = "Screw driver" , Value = "Screw driver" }

           }, "Select Item")

            <div class="form-group">
                <label asp-for="Quantity" class="control-label"></label>
                <input asp-for="Quantity" class="form-control" />
                <span asp-validation-for="Quantity" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Price" class="control-label"></label>
                <input asp-for="Price" class="form-control" />
                <span asp-validation-for="Price" class="text-danger"></span>
            </div>
            <!--           <div class="form-group">
                   <label asp-for="Totalqty" class="control-label"></label>
                   <input asp-for="Totalqty" class="form-control" />
                   <span asp-validation-for="Totalqty" class="text-danger"></span>
               </div>
    -->
            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
